import React from 'react';
import { Props } from './interface';
import cn from 'classnames';
import t from 'prop-types';
import './style';

const Modal: React.FC<Props> = ({ children, style, className, ...rest }) => {
  const classNames = cn({});
  return <>Modal</>;
};

export default Modal;
