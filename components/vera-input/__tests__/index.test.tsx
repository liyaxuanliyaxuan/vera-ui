import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import VeraInput from '../index';

describe('VeraInput', () => {
   it('renders without error', () => {

   });
});
